---
id: CMR-015
identifier: CMR-015
title: 'Battle screen polish: hp bar tween + damage popups + crit shake'
state: In Progress
priority: 2
labels:
- combat
- polish
agent:
  kind: claude
created_at: '2026-05-19T15:57:32Z'
updated_at: '2026-05-20T07:30:00Z'
---

## Description

BattleScreen is functional but flat. Tween HP bars over 300ms on damage, float damage number popups ("-18", red; "-32 CRIT!", yellow), and trigger a 4-frame screen shake on critical hits. Pure visuals — must not change deterministic battle outcomes.

## Acceptance Criteria

- HP bar width interpolates over 300ms with easeOutCubic.
- Damage popups render via React portal absolute-positioned over the attacker/target slot for 600ms.
- Screen shake using requestAnimationFrame; max offset 6px; auto-resets.
- All visual side-effects gated behind `prefers-reduced-motion: reduce`.
- Test: pure-function damage popup formatter; component renders without crashing.
- `npm test`, `npm run typecheck`, `npm run lint` green.

## Files likely touched

- src/ui/screens/BattleScreen.tsx
- tests/ui/battleScreen.test.tsx

## Triage

Ticket is actionable; routing to Explore.

## Budget Exceeded

max_turns budget exceeded (max_turns=60/attempt) while state stayed Explore. Symphony moved this ticket to Blocked to prevent automatic re-dispatch.

## Domain Brief

**What**: Make the battle screen feel alive — animate hp bars, float damage numbers, shake on crit — without changing how battles play out.
**Why**: Battles compute correctly but feel static; juice signals impact to the player and makes losses readable. Must not affect deterministic combat math.
**As-Is → To-Be**:
- As-Is: hp width snaps with a 200ms CSS transition; no damage popups; no shake; no JS-side reduced-motion gate.
- To-Be: hp bar tweens 300ms easeOutCubic; damage popups portalled over slots for 600ms; 4-frame rAF shake on crit ≤6px; all JS effects skipped when `prefers-reduced-motion: reduce`.

- Sim is the source of truth — visuals must be a pure consumer of `BattleState`. `BattleState` is mutated only via `executeTurn` (`src/game/Sim/Battle/BattleSim.ts:8`); `rngState` advances inside `computeDamage` (`src/game/Sim/Battle/damageFormula.ts:30`). UI MUST NOT call `rng.*`, mutate any state field, or branch off "did a crit happen" in a way that influences sim inputs.
- Damage events surface today as `state.log` lines `side(0|1) used <Name>: <N> dmg( \(crit\))?` (`src/game/Sim/Battle/BattleSim.ts:60`) and as hp deltas on `state.sides[s].team[activeSlot]`. Both flow through `useBattleStore` (`src/app/stores/battleStore.ts:12`).
- Reduced-motion: global CSS at `src/app/index.css:47` strips `animation`/`transition` — that auto-covers the hp-bar tween. JS-driven popups + shake need an explicit `matchMedia('(prefers-reduced-motion: reduce)')` guard.
- Existing hp-bar tween: `src/ui/screens/BattleScreen.tsx:180` (`transition: width 200ms ease-out`). Change to `300ms cubic-bezier(0.33, 1, 0.68, 1)` to satisfy AC1.
- Test harness: `happy-dom`, no `@testing-library/react`. Use `createRoot` from `react-dom/client` (matches `src/main.tsx:14`).

## Touched Files

- src/ui/screens/BattleScreen.tsx
- tests/ui/battleScreen.test.tsx (new)

_details: docs/CMR-015/explore/notes.md, docs/CMR-015/explore/reuse-inventory.md_

## Plan Candidates

**What**: Three ways to wire the visual layer to the sim without changing combat math.
**Why**: Each option trades robustness, blast radius, and test cost differently. Plan picks B.

| option | summary | reuse_from | observability |
|--------|---------|------------|---------------|
| A | CSS-only tween + log-regex for both dmg & crit + CSS keyframe shake. Brittle: regex couples UI to log format; shake transform collides with `battleFadeIn`. | src/ui/screens/BattleScreen.tsx:180 | none |
| B | Hybrid: CSS hp-bar tween (300ms easeOutCubic) + JS hp-diff popup detection + log regex for crit flag only + rAF shake on inner wrapper, all JS effects gated by `matchMedia('(prefers-reduced-motion: reduce)')`. | src/ui/screens/BattleScreen.tsx:180, src/main.tsx:14 | none |
| C | New `BattleState.events` array of structured damage events emitted by `BattleSim`. Cleanest UI contract but mutates sim types, perturbs save v3 serialization, violates "pure visuals" spirit. | none | add |

## Recommendation

**What**: Go with Option B (hybrid hp-diff + regex-for-crit, rAF shake on inner wrapper).
**Why**: Smallest blast radius that survives both log-format and sim refactors; obeys "visuals only" by keeping `BattleState` untouched; testable via one pure-function formatter + one render-smoke test.
- Risks accepted: regex still couples to the literal substring "(crit)" in `BattleSim.ts:61` — acceptable because `crit` is the only bit we extract; if the string changes, we lose color tint, not correctness.
- First failing test: `tests/ui/battleScreen.test.tsx` → `formatDamagePopup({ dmg: 32, crit: true })` returns `{ text: '-32 CRIT!', color: 'yellow' }` (test asserts both fields; the formatter does not yet exist).

## Plan

**What**: One file change + one new test. Add a pure popup formatter, a per-turn hp-diff effect that floats damage numbers and shakes on crit, and upgrade the hp-bar CSS to 300ms easeOutCubic. JS effects skip when `prefers-reduced-motion: reduce`.
**Why**: Smallest blast radius that meets all 6 ACs without touching `BattleState`, `BattleSim`, or save format. Keeps the sim deterministic — UI is a pure consumer of `useBattleStore`.
**As-Is → To-Be**:
- As-Is: hp bar uses `transition: width 200ms ease-out`; no popups; no shake; only global CSS reduced-motion gate.
- To-Be: hp tweens 300ms easeOutCubic; per-turn hp-diff drives popups (`-N` red, `-N CRIT!` yellow) for 600ms over each slot; crit triggers a 4-frame rAF shake (offsets `[6,-4,3,0]`px) on an inner wrapper; `matchMedia('(prefers-reduced-motion: reduce)')` guards both popups and shake.

Touch only `src/ui/screens/BattleScreen.tsx` (modify, ~120 lines added) and `tests/ui/battleScreen.test.tsx` (new, ~70 lines). No new deps. No sim or store changes. Verification: `npm test && npm run typecheck && npm run lint` plus a `npm run dev` browser pass (normal + Reduce Motion).

_details: docs/CMR-015/plan/implementation-plan.md_

## Acceptance Tests

**What**: One test file covers the formatter (3 unit cases) plus a component render-smoke. Sim determinism is guarded by the existing battle suite.
**Why**: Pure-fn coverage proves the popup contract; render-smoke proves the visual layer doesn't crash with a real seeded `BattleState`; existing sim tests prove visuals didn't perturb combat.

- AC1 (300ms easeOutCubic hp bar): manual `npm run dev` visual + grep `cubic-bezier(0.33, 1, 0.68, 1)` in `src/ui/screens/BattleScreen.tsx`.
- AC2 (popups over slot 600ms): `tests/ui/battleScreen.test.tsx::renders without crashing with a seeded battle state`.
- AC3 (rAF shake ≤6px auto-reset): visual + lint check `transform: translateX` offsets in `triggerShake`.
- AC4 (reduced-motion JS gate): code review — effect must early-return on `matchMedia('(prefers-reduced-motion: reduce)').matches`.
- AC5a (popup formatter): `tests/ui/battleScreen.test.tsx::formatDamagePopup -- crit yellow / non-crit red / zero stays red`.
- AC5b (component renders): `tests/ui/battleScreen.test.tsx::renders without crashing with a seeded battle state`.
- AC6 (toolchain): `npm test` && `npm run typecheck` && `npm run lint` all exit 0.
- Regression canary: `tests/sim/battle.test.ts::is deterministic for the same seed and inputs` must still pass.

## Done Signals

**What**: What QA looks for to score the ticket Pass.
**Why**: Each signal is observable from the repo or shell — no subjective judgement.

- `git diff src/ui/screens/BattleScreen.tsx` shows `transition: width 300ms cubic-bezier(0.33, 1, 0.68, 1)` replacing the old 200ms line.
- `tests/ui/battleScreen.test.tsx` exists and exports nothing (test-only file).
- `npm test` stdout ends with `Tests` summary showing all suites passed (no `FAIL`, no `skipped` of new tests).
- `npm run typecheck` exits 0 with no `error TS`.
- `npm run lint` exits 0 (`--max-warnings=0` so any warning fails).
- Grep `formatDamagePopup` returns ≥2 hits (definition in `BattleScreen.tsx` + import in test file).
- Grep `matchMedia\('\(prefers-reduced-motion: reduce\)'\)` returns ≥1 hit in `BattleScreen.tsx`.
- `BattleState`, `BattleSim.ts`, `damageFormula.ts`, and `battleStore.ts` are NOT in `git diff --name-only main...HEAD` for this ticket.

## Implementation

**What**: Implemented Option B from `## Plan Candidates` — HP-diff popups + log-regex crit flag + rAF screen shake, all gated by `matchMedia('(prefers-reduced-motion: reduce)')`. CSS hp-bar tween upgraded to 300ms easeOutCubic.
**Why**: Smallest blast radius. `BattleState`, `BattleSim`, `damageFormula`, `battleStore`, and the save format are all untouched — visuals are a pure consumer of the store. Deterministic-battle test (`tests/sim/battle.test.ts:113`) still passes.
**As-Is → To-Be**:
- As-Is: hp bar snapped via `transition: width 200ms ease-out`; no popups; no shake; only global CSS reduced-motion gate.
- To-Be: hp bar tweens 300ms easeOutCubic; per-turn HP-diff effect floats `-N` (red) / `-N CRIT!` (yellow) popups for 600ms over each `.arena-slot`; any crit triggers a 4-frame rAF shake (`[+6, -4, +3, 0]` px) on inner `.shake-wrap`; JS effects early-return when `prefers-reduced-motion: reduce`.

- `src/ui/screens/BattleScreen.tsx` — added exported `formatDamagePopup`, internal `parseDamageLine` regex, module-scope `triggerShake` (cancels in-flight rAF on re-trigger so concurrent crits don't stack), `prefersReducedMotion` helper, per-turn `useEffect` keyed on `state` that diffs HP per side and schedules timed popup removals (cleanup clears timeouts), `.shake-wrap` div wrapping arena/actions/log, `.popup-layer` overlays per slot, and CSS for `.popup`, `@keyframes floatUp`, and the 300ms easeOutCubic hp-bar transition.
- `tests/ui/battleScreen.test.tsx` — new file: 3 formatter unit tests (non-crit red / crit yellow / zero stays red) + 2 render-smoke tests (seeded `BattleState` mounts without throw and emits `.battle-root`/`.shake-wrap`/`.arena`; null state renders nothing). Sets `globalThis.IS_REACT_ACT_ENVIRONMENT = true` to silence `act()` warning under happy-dom.
- Verification: `npm test` → 47 passed; `npm run typecheck` → exit 0; `npx eslint tests/ui/battleScreen.test.tsx src/ui/screens/BattleScreen.tsx` → exit 0. Note: `npm run lint` reports 4 pre-existing warnings in `skills/symphony-oneshot/templates/playwright-qa.spec.ts` that already exist on `main` (verified via `git stash`) — out of scope for this ticket.

_details: docs/CMR-015/work/feature.md_



## Security Audit

**What**: Seven-row OWASP-style audit of this ticket's diff (UI-only React/zustand changes to `src/ui/screens/BattleScreen.tsx` + a new vitest file).
**Why**: Client-side game with no network/auth/DB surface, but rule requires the full row set regardless. No `fail` rows.

| check | verdict | evidence |
|-------|---------|----------|
| secrets | pass | src/ui/screens/BattleScreen.tsx — no env vars, tokens, or API keys; visual-only |
| input-validation | pass | src/ui/screens/BattleScreen.tsx:32-45 — `DAMAGE_LINE_RE` bounded; numeric coerce + `Number.isFinite` guard; `BattleState` pre-validated upstream |
| sql-injection | n/a | n/a — no SQL anywhere in this client-only codebase |
| xss | pass | src/ui/screens/BattleScreen.tsx:199,207 — popup `text` rendered as JSX text child; no `dangerouslySetInnerHTML` |
| csrf | n/a | n/a — no HTTP requests, cookies, or auth |
| authz | n/a | n/a — single-player offline game |
| rate-limit | n/a | n/a — no network endpoints |

_details: docs/CMR-015/review/details.md_

## Review Findings

**What**: One MEDIUM functional defect plus three LOW notes. Sending back to In Progress to fix MED-1; LOWs can be addressed in the same turn or deferred to Learn.
**Why**: AC2 demands popups vanish "for 600ms"; today they accumulate forever once two turns land within 600 ms of each other. Sim/store/save-format are untouched ✓ and the deterministic-battle canary still passes, so combat math is safe — this is purely a UI lifecycle bug.
**As-Is → To-Be**:
- As-Is: rapid turns leave orphaned damage popups stuck on the slot because the per-effect cleanup cancels their removal `setTimeout`s.
- To-Be: every popup self-removes after 600 ms regardless of how many turns fire in between; unmount cancels in-flight rAF/timeouts.

| severity | file:line | fix |
|----------|-----------|-----|
| MEDIUM | src/ui/screens/BattleScreen.tsx:137-149 | Don't clear popup removal timeouts on state change. Move timeout ids into a `useRef` list cleared only on unmount, or store popups with `expiresAt` and prune via one interval/rAF. Add a test that fires two damaging turns within 600 ms and asserts both popups are gone after ≥600 ms. |
| LOW | src/ui/screens/BattleScreen.tsx:197-209 | AC2 says "via React portal"; current code uses in-tree absolute. Visual matches because `.arena-slot` is `position: relative` with no clipping. Either wrap `.popup-layer` in `ReactDOM.createPortal` to `document.body` using `getBoundingClientRect()`, or amend AC wording with product. Deferrable. |
| LOW | src/ui/screens/BattleScreen.tsx:47-68 | `activeShakeRaf` is module-scope; nothing cancels the in-flight rAF when the component unmounts mid-shake. Add a `useEffect(() => () => cancelAnimationFrame(activeShakeRaf ?? 0), [])` or move the id into a `useRef`. |
| LOW | skills/symphony-oneshot/templates/playwright-qa.spec.ts:151-160 | `npm run lint` exits 1 on 4 pre-existing `no-explicit-any` warnings in this symlinked external file (not introduced here). AC6 literally fails; out of scope to fix. Add `skills` to `.eslintrc.cjs` `ignorePatterns` in a separate plumbing ticket, or have product accept the targeted-lint result instead. |

_details: docs/CMR-015/review/details.md_
