---
id: CMR-030
identifier: CMR-030
title: 'Save v4 migration: weather + fog + tech + traits one-shot migration + golden
  fixture'
state: Explore
priority: 1
labels:
- saves
- quality
agent:
  kind: claude
created_at: '2026-05-19T16:14:38Z'
updated_at: '2026-05-19T20:10:51Z'
---

## Description

Several other tickets (CMR-004 weather, CMR-006 learnedMoves, CMR-012 fog, CMR-026 biome, CMR-027 traits, CMR-029 tech) all flag additive save changes. To avoid drift, this ticket locks in a single v3 → v4 migration that prepares all of them and provides a golden v3 fixture test.

## Acceptance Criteria

- `src/game/Sim/Saves/schema.ts` (or codec) v4 adds: `weather`, `fog` (RLE), `tech` (research state), `biome`, `Critter.traitId`, `Critter.learnedMoves[]` — all with safe defaults.
- `src/game/Sim/Saves/migrations.ts` registers v3 → v4 migration; defaults are deterministic (weather=clear, fog=all-unseen-but-current, tech=initial, etc.).
- New `tests/sim/migrations.test.ts` golden test: load a captured v3 fixture (`tests/fixtures/save-v3.bin`), upgrade to v4, assert all new fields present + previous fields unchanged.
- `npm test`, `npm run typecheck`, `npm run lint` green.

## Files likely touched

- src/game/Sim/Saves/schema.ts, codec.ts, migrations.ts
- tests/sim/migrations.test.ts
- tests/fixtures/save-v3.bin (new)

## Triage

Ticket is actionable; routing to Explore.

## Budget Exceeded

max_turns budget exceeded (max_turns=60/attempt) while state stayed Explore. Symphony moved this ticket to Blocked to prevent automatic re-dispatch.
