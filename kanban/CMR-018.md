---
id: CMR-018
identifier: CMR-018
title: 'Performance overlay: per-system ms + entity count + FPS (DEV only)'
state: Explore
priority: 2
labels:
- performance
- dev
agent:
  kind: claude
created_at: '2026-05-19T15:57:32Z'
updated_at: '2026-05-19T20:10:51Z'
---

## Description

Add a DEV-mode overlay (toggleable with `F3`) showing rolling 60-frame averages of: per-system ms (needs / behavior / jobs / pathing / movement / construct / turret / raid / wildAi / time), total entity count, FPS, pending path requests. Disabled in production builds (tree-shaken).

## Acceptance Criteria

- New `src/game/Profiler.ts` with `section(name, fn)`.
- Each system wrapped in `section`.
- Overlay renders a fixed top-right panel; F3 toggles.
- Vite `define` strips overlay in prod.
- Test: profiler accumulates ms; export shape stable.
- `npm test`, `npm run typecheck`, `npm run lint` green.

## Files likely touched

- src/game/Profiler.ts (new)
- src/game/Game.ts (wrap systems)
- src/ui/panels/PerfOverlay.tsx (new)
- vite.config.ts (define)
- tests/game/profiler.test.ts

## Triage

Ticket is actionable; routing to Explore.

## Budget Exceeded

max_turns budget exceeded (max_turns=60/attempt) while state stayed Explore. Symphony moved this ticket to Blocked to prevent automatic re-dispatch.
