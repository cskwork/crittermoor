---
id: CMR-020
identifier: CMR-020
title: 'Determinism replay test: action log + hash compare'
state: Explore
priority: 1
labels:
- quality
- saves
agent:
  kind: claude
created_at: '2026-05-19T15:57:32Z'
updated_at: '2026-05-19T20:10:51Z'
---

## Description

Determinism is core to the design but there is no automated replay test. Add a thin action log (player tool + timestamps), a replayer that runs the recorded log against a fresh world, and a hash compare of the final SimWorld state. Lock down future drift.

## Acceptance Criteria

- New `src/game/Sim/replay.ts` with `recordAction(action)` / `replay(seed, actions)` / `hashWorld(world)`.
- `tests/sim/determinism.test.ts`: for a curated 200-tick scenario (chop + tame + battle + save + reload + 100 more ticks), the hash is stable across runs.
- Save schema gets an optional `actionLog` field (additive in v3).
- `npm test`, `npm run typecheck`, `npm run lint` green.

## Files likely touched

- src/game/Sim/replay.ts (new)
- src/game/Sim/Saves/codec.ts (additive)
- tests/sim/determinism.test.ts (new)

## Triage

Ticket is actionable; routing to Explore.

## Budget Exceeded

max_turns budget exceeded (max_turns=60/attempt) while state stayed Explore. Symphony moved this ticket to Blocked to prevent automatic re-dispatch.
